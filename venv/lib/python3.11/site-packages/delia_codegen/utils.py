from keyword import kwlist


def adl_name_to_py_name(name):
    py_name = str(name).replace('.', '_').lower()
    # if py_name is Python keyword, prefix py_name with '_'
    if (py_name in kwlist) or (py_name.endswith('_') and py_name[:-1] in kwlist):
        py_name += '_'
    return py_name
