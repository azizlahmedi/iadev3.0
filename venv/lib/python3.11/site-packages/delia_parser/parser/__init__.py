import delia_commons.exceptions
import delia_parser.cparser
import logging
from types import GeneratorType

logger = logging.getLogger('delia.parser')


def parse(compile_ctx, tokens, parser):
    assert(isinstance(tokens, GeneratorType))
    delia_parser.cparser.do_parser(compile_ctx, tokens, parser)
